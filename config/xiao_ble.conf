CONFIG_ZMK_KEYBOARD_NAME="Xiao-Test"
CONFIG_ZMK_BLE=y
CONFIG_ZMK_USB=y

# === 対策：切断後の待機時間を短くする ===
# スリープを有効にする（必須）
CONFIG_ZMK_SLEEP=y

# 放置（または切断）してからスリープするまでの時間を「30秒」にする
# 単位はミリ秒（30000 = 30秒）
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=30000
