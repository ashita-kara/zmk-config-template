# === Bluetooth基本設定 ===
CONFIG_ZMK_BLE=y
CONFIG_ZMK_USB=y

# === 【最重要】強制リセット＆発信 ===
# 起動時に過去のペアリング情報を消去し、強制的に検索モードにします
CONFIG_ZMK_BLE_CLEAR_BONDS_ON_START=y

# === バッテリー設定（単体用） ===
# ※SPLIT（分離）の記述は絶対に入れないでください
CONFIG_ZMK_BATTERY_REPORTING=y

# === 電波強度最大 ===
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
